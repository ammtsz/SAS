import styled from "styled-components";
import { colorRed } from "../../../assets/css/variables";

export const ErrorMessage = styled.p`
  margin-bottom: 10px;
  margin-left: 5px;

  color: ${colorRed};
  font-style: italic;
  font-size: 0.8rem;
`;
