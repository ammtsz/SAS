import styled from "styled-components"
import { colorRed } from "../../../../components/UI/variables"

export const ErrorMessage = styled.p`
    color: ${colorRed};
    font-style: italic;
    font-size: 0.8rem;
    margin-top: 5px;
    margin-left: 5px
`
